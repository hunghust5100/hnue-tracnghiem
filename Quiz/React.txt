Câu 1: Điểm khác biệt cốt lõi giữa Single Page Application (SPA) và Multi-Page Application (MPA) khi người dùng yêu cầu thông tin mới là gì?

A. MPA thực hiện hầu hết giao diện ở trình duyệt, SPA thực hiện ở server.

*B. MPA tải lại toàn bộ trang từ server, trong khi SPA chỉ yêu cầu những phần dữ liệu/giao diện cần thiết và cập nhật động mà không reload trang.

C. SPA tải lại toàn bộ tài nguyên (hình ảnh, CSS) ở mỗi yêu cầu.

D. MPA không bao giờ gửi yêu cầu lên server sau lượt tải đầu tiên.

Giải thích: Theo nội dung slide 5 và 6, ứng dụng đa trang truyền thống (MPA) yêu cầu tải lại toàn bộ phiên bản mới của trang từ server (Full reload), trong khi ứng dụng đơn trang (SPA) chỉ yêu cầu những mảnh thông tin cần thiết qua Web API và cập nhật động (No reload).

Câu 2: Lợi ích nào sau đây KHÔNG phải là ưu điểm của Single Page Application (SPA) so với Multi-Page Application (MPA)?

A. Trải nghiệm người dùng mượt mà và nhanh chóng hơn.

B. Giảm tải cho server sau lượt tải đầu tiên.

*C. Tối ưu hóa SEO dễ dàng hơn mà không cần cấu hình phức tạp.

D. Phù hợp cho các ứng dụng web động và có tính tương tác cao.

Giải thích: Theo bảng so sánh ở slide 8, việc tối ưu hóa SEO khó khăn hơn (Harder SEO optimization) chính là một nhược điểm lớn của SPA chứ không phải ưu điểm, do SPA phụ thuộc nhiều vào Client-side JavaScript.

Câu 3: Nhược điểm chính của Single Page Application (SPA) được chỉ ra trong tài liệu bài giảng là gì?

A. Tốn nhiều băng thông hơn MPA cho mỗi lượt chuyển trang tiếp theo.

*B. Tối ưu hóa SEO khó khăn hơn, lượt tải đầu tiên lớn và phụ thuộc nặng nề vào JavaScript phía client.

C. Server phải xử lý và dựng lại toàn bộ giao diện HTML cho mỗi click của người dùng.

D. Không thể áp dụng các tiêu chuẩn bảo mật phía server.

Giải thích: Slide 8 nêu rõ nhược điểm của SPA bao gồm: Khó tối ưu hóa SEO hơn, tải lượng tài nguyên ban đầu lớn (Large initial load) và dựa dẫm hoàn toàn vào client-side JavaScript.

Câu 4: Các ứng dụng phổ biến nào sau đây được bài giảng đưa ra làm ví dụ tiêu biểu cho kiến trúc Single Page Application (SPA)?

*A. Gmail, Facebook, Tiki.

B. Các trang blog tĩnh hoàn toàn bằng HTML thuần không sử dụng mã Script.

C. Các diễn đàn truyền thống bắt buộc tải lại trang mỗi khi bấm xem bài viết mới.

D. Các trang tin tức tĩnh phân tách bằng các thư mục vật lý trên máy chủ.

Giải thích: Slide 7 liệt kê trực tiếp ba ví dụ minh họa trực quan cho Single Page App bao gồm Gmail, Facebook và trang thương mại điện tử Tiki.

Câu 5: Trong môi trường phát triển React hiện đại, công cụ Vite thường được sử dụng nhằm mục đích thay thế cho công cụ nào sau đây?

A. Next.js

*B. Create React App (CRA)

C. Redux

D. React Native

Giải thích: Slide 9 chỉ ra rằng Vite là một công cụ xây dựng frontend hiện đại, tốc độ cao và hiện nay thường xuyên thay thế cho Create React App (CRA) khi thiết lập các dự án React mới.

Câu 6: Cú pháp JSX (JavaScript XML) trong React có đặc điểm cốt lõi nào sau đây?

A. Trình duyệt web có thể trực tiếp thực thi mã JSX mà không cần qua bất kỳ quá trình biên dịch nào.

*B. Là một phần mở rộng cú pháp của JavaScript cho phép viết mã có cấu trúc giống HTML bên trong JavaScript, sau đó được biên dịch thành các hàm JavaScript thuần.

C. Là một ngôn ngữ lập trình độc lập thay thế hoàn toàn vai trò của JavaScript trong ứng dụng Web.

D. Là cú pháp đặc quyền chỉ dùng riêng để định nghĩa kiểu dáng và giao diện CSS cho component.

Giải thích: Slide 9 định nghĩa JSX là cú pháp mở rộng của JavaScript, giúp viết giao diện giống HTML trong mã nguồn và sau đó sẽ được transpiled (biên dịch) thành các hàm JavaScript thông thường để trình duyệt hiểu được.

Câu 7: Công cụ xây dựng Vite đạt được tốc độ biên dịch và thực thi cực nhanh cho ứng dụng React nhờ vào yếu tố kỹ thuật nào?

A. Đồng bộ hóa toàn bộ cơ sở dữ liệu Real DOM của trình duyệt lên bộ nhớ đệm đám mây.

*B. Sử dụng cơ chế ES Modules thuần và esbuild cho quá trình biên dịch.

C. Chuyển đổi toàn bộ logic hiển thị của ứng dụng chạy hoàn toàn trên máy chủ.

D. Loại bỏ hoàn toàn mã JavaScript và chuyển đổi ứng dụng thành một trang tĩnh hoàn toàn.

Giải thích: Slide 9 giải thích Vite có tốc độ biên dịch siêu nhanh nhờ tận dụng sức mạnh của ES Modules gốc (Native ES Modules) kết hợp với công cụ biên dịch esbuild.

Câu 8: Nhược điểm lớn nhất của Real DOM (DOM truyền thống của trình duyệt) khi ứng dụng có nhiều cập nhật liên tục là gì?

A. Không thể biểu diễn cấu trúc dưới dạng cây phân cấp dữ liệu phần tử.

*B. Hiệu năng kém do trình duyệt phải tính toán lại kiểu dáng, reflow bố cục và repaint lại toàn bộ hoặc một phần lớn trang web khi có thay đổi.

C. Ngăn chặn hoàn toàn JavaScript truy cập và thao tác chỉnh sửa nội dung các thẻ HTML.

D. Tiêu tốn dung lượng lưu trữ ổ cứng của người dùng thay vì sử dụng bộ nhớ RAM.

Giải thích: Dựa vào slide 12, Real DOM có giới hạn về hiệu năng (Poor performance) vì mỗi khi có sự thay đổi, trình duyệt bắt buộc phải tính toán lại style, reflow layout và repaint lại giao diện diện rộng, dẫn đến hiện tượng lag giật.

Câu 9: Bản chất của khái niệm Virtual DOM trong React được định nghĩa là gì?

A. Một tệp cấu hình định dạng XML lưu trữ cố định trên máy chủ ứng dụng.

*B. Một đối tượng JavaScript gọn nhẹ nằm trong bộ nhớ RAM, đóng vai trò là đại diện in-memory của Real DOM.

C. Một tiện ích mở rộng bắt buộc người dùng phải cài đặt thêm vào trình duyệt để chạy ứng dụng React.

D. Thẻ HTML nguyên bản dùng để bao bọc toàn bộ mã nguồn hiển thị của trang web.

Giải thích: Slide 13 định nghĩa rõ ràng Virtual DOM là một đối tượng JavaScript, cấu trúc in-memory gọn nhẹ nằm trong RAM chứ không can thiệp trực tiếp vào cấu trúc văn bản vật lý của trình duyệt.

Câu 10: Quy trình xử lý 3 bước của Virtual DOM trong React khi có sự thay đổi dữ liệu diễn ra theo thứ tự chuẩn nào?

A. Patch -> Diff -> Render

B. Diff -> Render -> Patch

*C. Render -> Diff -> Patch

D. Render -> Patch -> Diff

Giải thích: Slide 14 phác thảo quy trình bao gồm 3 bước tuần tự: Step 1: Render (Tạo cây Virtual DOM mới), Step 2: Diff (So sánh tìm sự khác biệt tối thiểu) và Step 3: Patch (Áp dụng các thay đổi đó lên Real DOM).

Câu 11: Trong quy trình cập nhật giao diện của React, bước "Diff" thực hiện nhiệm vụ trọng tâm nào?

A. Tạo ra một cây Virtual DOM hoàn toàn mới dựa trên dữ liệu cập nhật từ API.

*B. Sử dụng thuật toán đối sánh để so sánh cây Virtual DOM mới với phiên bản trước đó nhằm xác định tập hợp các thay đổi tối thiểu cần thiết.

C. Áp dụng trực tiếp và ngay lập tức toàn bộ mã giao diện mới đè lên cây Real DOM.

D. Xóa bỏ toàn bộ bộ nhớ đệm RAM để ép trình duyệt phải tải lại trang hoàn toàn từ đầu.

Giải thích: Theo slide 14, bước "Diff" áp dụng thuật toán đối sánh để đối chiếu cây Virtual DOM mới và cũ, từ đó bóc tách ra các phần tử thực sự thay đổi để tối ưu hóa việc cập nhật.

Câu 12: Bộ máy đối sánh React Fiber (được giới thiệu từ phiên bản React 16) mang lại cải tiến đột phá nào cho quá trình cập nhật giao diện?

A. Ép buộc quá trình cập nhật phải diễn ra đồng bộ, không cho phép ngắt quãng để đảm bảo an toàn.

*B. Thay đổi quá trình cập nhật từ đồng bộ sang bất đồng bộ, có thể ngắt quãng, trì hoãn và phân bổ thứ tự ưu tiên cho các tác vụ quan trọng.

C. Loại bỏ hoàn toàn Virtual DOM để tương tác trực tiếp bằng mã thuần với Real DOM.

D. Giới hạn ứng dụng chỉ được phép sử dụng Class Component để quản lý vòng đời.

Giải thích: Slide 15 nêu rõ điểm cải tiến của React Fiber: thay đổi từ cập nhật đồng bộ (synchronous) sang bất đồng bộ (asynchronous), có khả năng ngắt quãng (interruptible) và ưu tiên tác vụ (prioritized).

Câu 13: Hãy xem xét đoạn mã khởi tạo phần tử JSX sau đây:

Kết quả chạy mã
File created successfully.

```javascript
const element = <h1>Hello, React!</h1>;

Bản chất của biến element sau khi mã được biên dịch (transpiled) bởi React là gì?

A. Một chuỗi HTML thuần (String) dùng để gán vào thuộc tính innerHTML.

B. Một nút phần tử vật lý thực tế nằm trên Real DOM của trình duyệt.

*C. Một đối tượng JavaScript mô tả cấu trúc giao diện (React element / JavaScript Object).

D. Một hàm thực thi trực tiếp việc thay đổi màu sắc font chữ hệ thống.

Giải thích: Slide 9 chỉ ra rằng JSX bản chất chỉ là một cú pháp mở rộng, khi biên dịch nó sẽ trở thành các hàm gọi JavaScript thuần để tạo ra các đối tượng JavaScript đại diện cho phần tử giao diện trong bộ nhớ.

Câu 14: Cho đoạn mã xây dựng Component sau:

JavaScript 
function Welcome() {
return (
<h1 style={{ color: "#1976d2", textAlign: "center" }}>
Hello, React!
</h1>
);
}

Cú pháp style={{ color: "#1976d2", textAlign: "center" }} được giải thích như thế nào?

A. Cặp ngoặc nhọn thứ nhất đại diện cho HTML style, cặp ngoặc thứ hai đại diện cho CSS style.

*B. Cặp ngoặc nhọn ngoài cùng dùng để viết mã JavaScript bên trong JSX, cặp ngoặc nhọn bên trong chứa một đối tượng JavaScript định nghĩa các thuộc tính CSS theo định dạng camelCase.

C. Đây là cú pháp gọi một lớp CSS (class selection) nằm ở tệp bên ngoài.

D. Đây là một lỗi cú pháp nghiêm trọng vì JSX không chấp nhận hai cặp dấu ngoặc nhọn đi liền kề nhau.

Giải thích: Slide 17 thể hiện đoạn mã chứa cú pháp style inline này. Trong JSX, ngoặc nhọn ngoài cùng dùng để bao bọc biểu thức JavaScript, còn cặp ngoặc nhọn trong là một Object chứa cặp key-value của thuộc tính CSS (viết dưới dạng camelCase như textAlign thay vì text-align).

Câu 15: Khi đối chiếu giữa Class Component và Functional Component, nhận định nào sau đây phản ánh chính xác cấu trúc lưu trữ và quản lý trạng thái?

A. Functional Component quản lý state bằng từ khóa this.state, còn Class Component sử dụng Hook useState().

*B. Class Component khai báo bằng từ khóa class, quản lý state qua this.state và setState(); Functional Component khai báo bằng function và sử dụng Hook useState().

C. Functional Component có mã nguồn dài dòng, phức tạp và khó đọc hơn hẳn Class Component.

D. Class Component sử dụng Hook useEffect() để quản lý trực tiếp các phương thức vòng đời như componentDidMount.

Giải thích: Dựa theo bảng so sánh toàn diện tại slide 19 và mã minh họa tại slide 20, Class Component dùng từ khóa class, quản lý state thông qua this.state / setState(), trong khi Functional Component dùng từ khóa function và dùng useState().

Câu 16: Cho đoạn mã Class Component quản lý bộ đếm sau:

JavaScript 
class Counter extends React.Component {
state = { count: 0 };
render() {
return (
<button onClick={() => this.setState({ count: this.state.count + 1 })}>
+1
</button>
);
}
}

Nếu chuyển đổi cấu trúc trên sang một Functional Component sử dụng React Hooks tương đương hoàn toàn, đoạn mã nào dưới đây viết đúng ngữ pháp?

A.

JavaScript 
function Counter() {
const count = 0;
return <button onClick={() => count++}>+1</button>;
}

*B.

JavaScript 
function Counter() {
const [count, setCount] = useState(0);
return <button onClick={() => setCount(count + 1)}>+1</button>;
}

C.

JavaScript 
function Counter() {
const [count, setCount] = useState(0);
return <button onClick={() => this.setState({ count: count + 1 })}>+1</button>;
}

D.

JavaScript 
function Counter() {
state = { count: 0 };
return <button onClick={() => setCount(count + 1)}>+1</button>;
}

Giải thích: Slide 20 cung cấp mã so sánh trực quan giữa hai loại component cho tính năng Counter này. Functional Component sử dụng cú pháp phá cấu trúc mảng từ Hook useState(0) để lấy biến trạng thái count và hàm setter setCount tương ứng.

Câu 17: Xem xét mô hình tương tác giữa các Component trong cây Component, cơ chế tương tác chuẩn từ "Component Cha xuống Component Con" và từ "Component Con lên Component Cha" lần lượt là gì?

A. Sử dụng State từ Cha xuống Con; Sử dụng Props từ Con lên Cha.

*B. Sử dụng Props từ Cha xuống Con; Sử dụng Callback Function từ Con lên Cha.

C. Sử dụng Callback Function từ Cha xuống Con; Sử dụng Props từ Con lên Cha.

D. Sử dụng Context API từ Cha xuống Con; Sử dụng State từ Con lên Cha.

Giải thích: Slide 21 liệt kê rõ ràng quy tắc tương tác: Hướng Parent -> Child truyền dữ liệu qua thuộc tính (Props); Hướng Child -> Parent truyền dữ liệu ngược lên bằng cách thực thi hàm gọi lại (Callback Function) được cha cung cấp qua sự kiện.

Câu 18: Cho đoạn mã thể hiện tương tác ngược từ con lên cha như sau:

JavaScript 
function Child({ onNotify }) {
return <button onClick={() => onNotify("Hello from Child!")}>Send</button>;
}

function Parent() {
const [childMsg, setChildMsg] = React.useState("");
const handleChildMessage = (msg) => setChildMsg(msg);
return (
<div>
<p>Message from Child: {childMsg}</p>
<Child onNotify={handleChildMessage} />
</div>
);
}

Khi người dùng bấm vào nút "Send" ở Component Child, diễn biến nào sau đây xảy ra?

A. Thuộc tính onNotify của Child sẽ tự động xóa khỏi bộ nhớ RAM.

*B. Hàm handleChildMessage tại Parent được kích hoạt với đối số truyền vào là chuỗi "Hello from Child!", cập nhật state childMsg và khiến Parent re-render.

C. Component Parent ngay lập tức báo lỗi biên dịch vì Component con không được phép truyền tham số cho Component cha.

D. Trình duyệt thực hiện Full reload để đồng bộ lại trạng thái.

Giải thích: Slide 22 minh họa kịch bản này, giải thích cơ chế tương tác Child-to-Parent. Hàm callback handleChildMessage được định nghĩa ở cha, truyền xuống con qua prop onNotify. Khi con kích hoạt hàm này, giá trị chuỗi được gửi ngược lên để hàm xử lý state ở cha thực thi, làm thay đổi UI.

Câu 19: Cho đoạn mã khai báo Component và sử dụng như sau:

JavaScript 
function UserCard(props) {
return <h3>Hello, {props.name}!</h3>;
}

function App() {
return (
<div>
<UserCard name="Lam" />
</div>
);
}

Khẳng định nào dưới đây phản ánh chính xác luồng di chuyển dữ liệu (Data Flow) của chuỗi "Lam"?

A. Di chuyển ngược từ UserCard lên thành phần App thông qua đối tượng state nội bộ.

*B. Di chuyển theo luồng một chiều duy nhất xuôi từ cha (App) xuống con (UserCard) dưới dạng thuộc tính (prop).

C. Di chuyển xoay vòng hai chiều liên tục không có điểm dừng giữa cha và con.

D. Dữ liệu đứng im và chỉ có thể được chỉnh sửa trực tiếp bởi chính hàm xử lý của UserCard.

Giải thích: Theo slide 23 và slide 27, Props tuân thủ nghiêm ngặt nguyên lý luồng dữ liệu một chiều (One-way data flow), dữ liệu luôn luôn chảy từ trên xuống (downward) - tức là từ Parent sở hữu dữ liệu truyền xuống Child hiển thị.

Câu 20: Đoạn mã nào sau đây cố tình vi phạm nguyên lý hoạt động "Chỉ đọc" (Read-only / Immutable) của Props trong React?

A.

JavaScript 
function UserCard(props) {
return <h3>Hello, {props.name}</h3>;
}

*B.

JavaScript 
function UserCard(props) {
props.name = "Changed!";
return <h3>Hello, {props.name}</h3>;
}

C.

JavaScript 
function UserCard(props) {
const currentName = props.name;
return <h3>Hello, {currentName}</h3>;
}

D.

JavaScript 
function UserCard({ name }) {
return <h3>Hello, {name}</h3>;
}

Giải thích: Slide 24 nhấn mạnh "Props are Read-only" và minh họa đoạn mã sai (Wrong) khi cố tình gán lại giá trị mới cho props.name = "Changed!". Lập trình viên không được phép trực tiếp đột biến (mutate) đối tượng props nhận được từ cha.

Câu 21: Cho đoạn mã Component con nhận nhiều giá trị thuộc tính như sau:

JavaScript 
function UserCard(props) {
return <h3 style={{ color: props.color }}>Hello, {props.name}!</h3>;
}

Để gọi Component này ở Component cha sao cho hiển thị dòng chữ "Hello, Thu!" với màu tím (purple), cú pháp nào sau đây là chính xác?

*A. <UserCard name="Thu" color="purple" />

B. <UserCard props={{ name: "Thu", color: "purple" }} />

C. <UserCard value="Thu" theme="purple" />

D. <UserCard color="purple">Thu</UserCard>

Giải thích: Dựa vào mã mẫu ở slide 25, Component cha truyền dữ liệu xuống component con bằng cách định nghĩa các cặp attribute tương ứng với tên property trên thẻ Component (name="Thu" color="purple"), component con truy cập qua props.propertyName.

Câu 22: Hãy xem xét đoạn mã sử dụng thuộc tính đặc biệt props.children sau đây:

JavaScript 
function Card(props) {
return <div style={{ border: "2px solid red" }}>{props.children}</div>;
}

function Button() {
return <button>Click Me</button>;
}

function App() {
return (
<Card>
<Button />
</Card>
);
}

Khi mã nguồn trên được render, phần giữ chỗ {props.children} bên trong cấu trúc của Card sẽ được thay thế bằng thành phần nào?

A. Một chuỗi ký tự text có nội dung là "props.children".

*B. Toàn bộ Component <Button /> (bao gồm thẻ button và dòng chữ Click Me).

C. Hoàn toàn trống rỗng do Card không truyền thuộc tính nào tên là children ở dạng attribute thông thường.

D. Thẻ div có đường viền màu đỏ nhưng không có ruột.

Giải thích: Slide 26 làm rõ khái niệm props.children, nó đại diện cho bất kỳ nội dung hay component nào được lồng (nested) ở giữa cặp thẻ mở và thẻ đóng của một component. Trong trường hợp này, <Button /> nằm giữa <Card> và </Card> nên nó chính là props.children.

Câu 23: Hãy phân tích đoạn mã xử lý sự kiện click của lập trình viên sau đây:

JavaScript 
function Counter() {
const [count, setCount] = useState(0);
return (
<div>
<p>Count: {count}</p>
<button onClick={() => count++}>+1</button>
</div>
);
}

Khi người dùng thực hiện bấm vào nút "+1", hiện tượng nào sẽ xảy ra trên màn hình ứng dụng?

A. Giá trị số hiển thị lập tức tăng lên thành 1 nhờ toán tử tăng tiến.

*B. Giao diện hoàn toàn giữ nguyên giá trị cũ (không thay đổi) do lập trình viên đã vi phạm nguyên tắc bất biến, sửa đổi trực tiếp biến state mà không dùng hàm setter setCount.

C. Ứng dụng báo lỗi cú pháp ngay khi trình duyệt phân tích file JavaScript.

D. Thành phần bộ đếm tự động giảm về giá trị âm.

Giải thích: Slide 31 đưa ra ví dụ sai kinh điển này (đánh dấu chữ X đỏ). Việc dùng biểu thức count++ là đang cố tình thay đổi trực tiếp biến state. React yêu cầu state là read-only đối với phép gán trực tiếp; giao diện chỉ re-render khi trạng thái được cập nhật thông qua hàm setter chuyên dụng (ví dụ: setCount).

Câu 24: Cho đoạn mã minh họa tính chất bất đồng bộ và cơ chế gom gói (batching) cập nhật trạng thái của React:

JavaScript 
function Counter() {
const [count, setCount] = useState(0);
function handleClick() {
console.log("Before:", count);
setCount(count + 1);
console.log("After:", count);
}
return <button onClick={handleClick}>+1</button>;
}

Giả định giá trị khởi tạo ban đầu của bộ đếm count đang là 0. Tại thời điểm người dùng click chuột vào nút "+1" lần đầu tiên, các thông điệp được in ra tại màn hình Console log lần lượt là gì?

A. Before: 0 và After: 1

B. Before: 1 và After: 1

*C. Before: 0 và After: 0

D. Before: 1 và After: 2

Giải thích: Bản mẫu log chạy thực tế ở slide 32 chứng minh rằng React không cập nhật giá trị của biến trạng thái ngay lập tức sau khi gọi hàm setter (setCount). Biến count trong phạm vi hàm handleClick hiện tại vẫn giữ nguyên giá trị tại thời điểm render đó (là 0). Do vậy, cả hai lệnh console.log trước và sau đều in ra giá trị 0. Giá trị mới bằng 1 chỉ xuất hiện ở lượt render tiếp theo.

Câu 25: Cho đoạn mã cố gắng tăng giá trị của biến trạng thái nhiều lần liên tiếp trong một hành động xử lý:

JavaScript 
function Counter() {
const [count, setCount] = useState(0);
function handleDoubleIncrease() {
setCount(count + 1);
setCount(count + 1);
}
return <button onClick={handleDoubleIncrease}>Double</button>;
}

Sau khi người dùng click vào nút "Double" đúng một lần duy nhất, giá trị số hiển thị của count thực tế trên giao diện màn hình sẽ tăng thêm bao nhiêu đơn vị?

A. Tăng thêm 2 đơn vị.

*B. Chỉ tăng thêm 1 đơn vị.

C. Không tăng đơn vị nào (vẫn giữ nguyên giá trị gốc là 0).

D. Giá trị tăng vô hạn gây tràn bộ nhớ trình duyệt.

Giải thích: Slide 41 giải thích rằng do hàm setter hoạt động bất đồng bộ và được gom gói lại (batched), cả hai câu lệnh setCount(count + 1) đều nhìn thấy giá trị count hiện tại là 0, nên chúng đều tính toán ra kết quả là 0 + 1 = 1. React gộp các yêu cầu này lại và chỉ cập nhật giá trị lên 1 ở lượt render mới.

Câu 26: Để khắc phục triệt để vấn đề gom gói dữ liệu ở câu hỏi 25, giúp giá trị count tăng chính xác thêm 2 đơn vị chỉ sau một lần click chuột, lập trình viên cần viết lại logic hàm handleDoubleIncrease theo cú pháp chuẩn nào dưới đây?

*A.

JavaScript 
function handleDoubleIncrease() {
setCount(prev => prev + 1);
setCount(prev => prev + 1);
}

B.

JavaScript 
function handleDoubleIncrease() {
count = count + 2;
setCount(count);
}

C.

JavaScript 
function handleDoubleIncrease() {
setCount(count + 2);
setCount(count + 2);
}

D.

JavaScript 
function handleDoubleIncrease() {
setCount(count + 1);
count = count + 1;
}

Giải thích: Giải pháp được slide 41 đưa ra chính là truyền một hàm callback vào hàm setter: setState(prev => ...). Khi dùng dạng hàm này, React sẽ truyền giá trị trạng thái chính xác, cập nhật nhất của bước trước đó vào tham số prev, giúp các lệnh tính toán xếp chồng hoạt động chính xác liên tiếp.

Câu 27: Đoạn mã nào sau đây cập nhật trạng thái kiểu mảng (Array State) tuân thủ hoàn hảo nguyên lý bất biến (Immutability), tạo ra một bản sao mới hoàn toàn thay vì đột biến mảng cũ?

A.

JavaScript 
const [numbers, setNumbers] = useState([1, 2, 3]);
function mutateAdd() {
numbers.push(4);
setNumbers(numbers);
}

*B.

JavaScript 
const [numbers, setNumbers] = useState([1, 2, 3]);
function immutableAdd() {
const newNumbers = [...numbers, 4];
setNumbers(newNumbers);
}

C.

JavaScript 
const [numbers, setNumbers] = useState([1, 2, 3]);
function addElement() {
numbers[numbers.length] = 4;
setNumbers(numbers);
}

D.

JavaScript 
const [numbers, setNumbers] = useState([1, 2, 3]);
function addElement() {
setNumbers(numbers.push(4));
}

Giải thích: Mã nguồn tại slide 42 phân tách rõ hai cách tiếp cận: hàm mutateAdd (Sai) dùng push trực tiếp phá vỡ tính bất biến; hàm immutableAdd (Đúng) sử dụng toán tử trải mảng (spread operator ...) sao chép toàn bộ phần tử cũ sang một mảng mới hoàn toàn rồi thêm phần tử số 4 vào cuối mảng, sau đó truyền mảng mới này vào hàm setter.

Câu 28: Hãy phân tích đoạn mã xử lý Form liên kết hai chiều (Controlled Input) sau đây:

JavaScript 
function FormExample() {
const [name, setName] = useState("");
function handleChange(event) {
setName(event.target.value);
}
return (
<div>
<input type="text" value={name} onChange={handleChange} />
<p>Hello, {name}!</p>
</div>
);
}

Khi người dùng gõ chuỗi chữ "Lam" vào ô nhập liệu, chuỗi vòng lặp sự kiện (Two-way Binding Flow) nào diễn ra bên trong ứng dụng?

A. React ép buộc re-render trước -> Gọi hàm handleChange -> State thay đổi ngầm định.

*B. Người dùng gõ phím -> Sự kiện onChange kích hoạt -> event.target.value lấy giá trị chữ mới -> Gọi hàm setName() cập nhật biến trạng thái -> React re-render lại component -> Phần tử input hiển thị chữ mới qua value={name} và thẻ p được cập nhật trên màn hình.

C. Thẻ p hiển thị trước chữ mới -> Trình duyệt báo hiệu cho input thay đổi -> Gọi hàm handleChange.

D. Ô input cập nhật cục bộ độc lập hoàn toàn mà không làm chạy lại hàm Component FormExample.

Giải thích: Sơ đồ chi tiết tại slide 43 (Two-way Binding) mô tả từng bước của luồng dữ liệu liên kết 2 chiều: Khi gõ chữ, sự kiện onChange nổ ra -> Lấy giá trị qua event.target.value -> Gọi setName() để ghi đè state -> React re-render lại cấu trúc -> Ô input lấy dữ liệu state mới đổ ngược lại thuộc tính value, tạo ra sự nhất quán dữ liệu.

Câu 29: Cho đoạn mã sử dụng useEffect để tải danh sách thông tin người dùng từ máy chủ:

JavaScript 
useEffect(() => {
fetch("[https://api.example.com/users](https://api.example.com/users)")
.then(res => res.json())
.then(data => setUsers(data));
}, []);

Mảng tham số thứ hai dạng rỗng [] (Empty dependency array) đặt ở cuối hàm useEffect đóng vai trò kỹ thuật quan trọng nào?

A. Khiến cho hàm effect liên tục chạy lại bất tận sau mỗi khi giao diện ứng dụng re-render.

*B. Đảm bảo hàm callback của effect thực thi duy nhất một lần duy nhất ngay sau khi component được mount (render thành công lần đầu tiên).

C. Kích hoạt chế độ kiểm thử tự động của React và ngăn chặn lệnh fetch chạy thực tế.

D. Dùng để truyền trực tiếp dữ liệu mảng trống vào biến trạng thái thay cho hàm setUsers.

Giải thích: Theo bảng tra cứu mảng phụ thuộc ở slide 46, mảng phụ thuộc rỗng [] biểu thị điều kiện effect "Only once on mount", thường áp dụng cho các tác vụ khởi tạo ban đầu như Fetch dữ liệu từ API bên ngoài hoặc thiết lập bộ đếm thời gian.

Câu 30: Hãy phân tích đoạn mã quản lý bộ đếm lặp tuần hoàn sử dụng hàm dọn dẹp (cleanup function) của useEffect sau:

JavaScript 
useEffect(() => {
const timer = setInterval(() => {
console.log("Running...");
}, 1000);

return () => {
clearInterval(timer);
console.log("Cleaned up!");
};
}, []);

Đoạn mã return () => { clearInterval(timer); }; đóng vai trò gì và sẽ được hệ thống React thực thi vào thời điểm nào?

A. Thực thi ngay lập tức ngay sau khi hàm setInterval vừa chạy xong câu lệnh khai báo thứ nhất.

B. Thực thi đều đặn lặp đi lặp lại sau mỗi chu kỳ 1000 mili giây đồng hành cùng lệnh in log.

*C. Đóng vai trò là hàm dọn dẹp (cleanup function), được chạy ngay trước khi component bị unmount khỏi giao diện màn hình nhằm hủy bỏ bộ đếm thời gian, tránh lỗi rò rỉ bộ nhớ.

D. Thực thi khi trạng thái nội bộ của biến timer xảy ra xung đột phần cứng.

Giải thích: Dựa trên slide 48, hàm trả về bên trong useEffect chính là hàm dọn dẹp (cleanup function). Nó chạy trước khi component bị unmount khỏi giao diện (hoặc trước lần chạy effect kế tiếp nếu có thay đổi dependency) mục đích nhằm hủy các tác vụ chạy ngầm như setInterval, giúp giải phóng tài nguyên hệ thống, ngăn ngừa rò rỉ bộ nhớ (memory leaks)."""

